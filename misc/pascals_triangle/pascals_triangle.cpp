#include <iostream>
#include <vector>

using namespace std;

vector<int> getRow(int rowIndex) {
  int row = 0;
  tur.  push_back(1);
  vector<int> row;
  while (row <= rowIndex) {

  }
}

int main(int argc, char const *argv[]) {
  /* code */

  int n;
  cin >> n;

  vector<int> tur;

  int a[10];
  for (size_t i = 0; i < 10; i++) {
    /* code */
    a[i] = i + 12;
  }


  return 0;
}
